// ===========================================================================
// DEPRECATED / REMOVED — SelfView (inset live-camera PiP).
// ---------------------------------------------------------------------------
// The whole screen is now the live player (PLAYER_RENDER_MODE 'webcam-fx' in
// config/game.config.js renders the mirrored webcam as the full-stage fighter),
// so the inset self-view picture-in-picture is redundant and was removed from
// the GameScreen HUD. This file is intentionally left empty of any export and
// is no longer imported anywhere. It is kept only because the host filesystem
// disallows deleting it; treat it as deleted.
// ===========================================================================

// (no exports — component intentionally removed)
